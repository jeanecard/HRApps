import { HrLocalStorageMockService } from './hr-local-storage-mock-service';

describe('HrLocalStorageMockService', () => {
  it('should create an instance', () => {
    expect(new HrLocalStorageMockService()).toBeTruthy();
  });
});
