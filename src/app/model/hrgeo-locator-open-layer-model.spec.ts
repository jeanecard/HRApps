import { HRGeoLocatorOpenLayerModel } from './hrgeo-locator-open-layer-model';

describe('HRGeoLocatorOpenLayerModel', () => {
  it('should create an instance', () => {
    expect(new HRGeoLocatorOpenLayerModel()).toBeTruthy();
  });
});
