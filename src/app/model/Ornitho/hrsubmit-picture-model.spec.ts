import { HRSubmitPictureModel } from './hrsubmit-picture-model';

describe('HRSubmitPictureModel', () => {
  it('should create an instance', () => {
    expect(new HRSubmitPictureModel()).toBeTruthy();
  });
});
