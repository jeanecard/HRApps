import { PopulationFilterModel } from './population-filter-model';

describe('PopulationFilterModel', () => {
  it('should create an instance', () => {
    expect(new PopulationFilterModel()).toBeTruthy();
  });
});
