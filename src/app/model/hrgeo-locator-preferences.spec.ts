import { HRGeoLocatorPreferences } from './hrgeo-locator-preferences';

describe('HRGeoLocatorPreferences', () => {
  it('should create an instance', () => {
    expect(new HRGeoLocatorPreferences()).toBeTruthy();
  });
});
