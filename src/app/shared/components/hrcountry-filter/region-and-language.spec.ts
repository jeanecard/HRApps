import { RegionAndLanguage } from './region-and-language';

describe('RegionAndLanguage', () => {
  it('should create an instance', () => {
    expect(new RegionAndLanguage()).toBeTruthy();
  });
});
