import { HRCountryFilterModel } from './hrcountry-filter-model';

describe('HRCountryFilterModel', () => {
  it('should create an instance', () => {
    expect(new HRCountryFilterModel()).toBeTruthy();
  });
});
