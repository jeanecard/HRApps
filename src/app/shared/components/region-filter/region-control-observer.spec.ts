import { RegionControlObserver } from './region-control-observer';

describe('RegionControlObserver', () => {
  it('should create an instance', () => {
    expect(new RegionControlObserver()).toBeTruthy();
  });
});
