import { TestBed } from '@angular/core/testing';

import { OpenLayerStylesService } from './open-layer-styles.service';

describe('OpenLayerStylesService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: OpenLayerStylesService = TestBed.get(OpenLayerStylesService);
    expect(service).toBeTruthy();
  });
});
